# This Pipfile is intended to create a general-purpose virtual environment for
# developing forge-keeper code.

[[source]]
url = 'https://pypi.python.org/simple'
verify_ssl = true
name = 'pypi'

[requires]
python_version = '3.8'

[packages]
connexion = ">=2.5.0"
swagger-ui-bundle = ">=0.0.2"
python-dateutil = ">=2.6.0"
flask = ">=1.1.1"
flask-cors = ">=3.0.8"
flask-sqlalchemy = ">=2.4.1"
psycopg2 = ">=2.8.4"
validators = "*"

[dev-packages]
pipreqs = "*"
coverage = "*"
